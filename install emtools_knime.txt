KNIME:

"Preferences-> Updates -> Available Software Sites -> Stable Community Contributions" needs to be enabled.

load the KNIME workflow cells_01 from the code repository
install all missing packages

Preferences -> KNIME -> Python, python 2 needs to to the anaconda installation directory.

Anaconda:

#start anaconda prompt, there type
conda install -c conda-forge tifffile numpy scipy pandas scikit-image
pip install mrcfile
pip install -e /path/to/package 
# provide the respective directory where emtools was dowloaded/cloned into.
# this will install emtools.py from the installation directory


